import type { Metadata } from "next";
import { PropsWithChildren } from "react";

export const metadata: Metadata = {
  title: "fortune(prize) Detail",
  description: "fortune(prize) Detail",
};

const fortune(prize)Layout = ({ children }: PropsWithChildren) => {
  return <div>{children}</div>;
};

export default fortune(prize)Layout;
